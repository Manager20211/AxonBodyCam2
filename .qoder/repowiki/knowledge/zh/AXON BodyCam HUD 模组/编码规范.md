- 常量集中定义：在类顶部统一定义 ARGB 颜色值、设备标识字符串及纹理 Identifier，避免魔法值散落在渲染逻辑中。
- 防御性渲染检查：在执行 HUD 绘制前，强制校验 `client.player` 非空及 `hudHidden` 状态，防止在无效状态下进行图形操作。
- 右对齐布局计算：通过 `TextRenderer` 动态测量文本宽度，以屏幕右侧为基准反向计算坐标，确保水印信息在不同分辨率下保持右对齐。